---
trigger: always_on
---

0. **Git Sync**: Before starting to update files, always pull latest code from GitHub (`git pull`) to prevent conflicts and ensure working on fresh source.
   0.1. **Scan Packages Repositories on Code Pull (RULE 53 — BẮT BUỘC KHI PULL CODE CÁC PACKAGES)**: When pulling code or synchronizing the workspace, it is mandatory to dynamically scan the `Packages/` directory for all git repositories (subdirectories containing `.git`). The workspace may have new repositories added (can have new packages or 0 newly added) at any time. Must pull latest code (`git pull`) across all discovered repositories in `Packages/` as well as the root workspace to guarantee all packages remain fresh and prevent divergence or conflicts (`npm run fetch:pull` / `node scripts/fetch-all.mjs --pull`).
   0.2. **Read Target Package README Before Update (RULE 64 — BẮT BUỘC ĐỌC README CỦA PACKAGE TRƯỚC KHI CẬP NHẬT BẤT KỲ FILE NÀO)**: Before making any modifications, updates, or adding features to any package in `Packages/`, developers and AI agents MUST read `Packages/{targetPackage}/README.md` first to understand the immutable target, architectural scope, and domain boundary of the app (e.g. Hub: drop drag build next.js to launcher apps; Office suite: read - edit xlsx, docs; SuperChat: mix chat from many apps). Strictly prohibited to update any package without reading its README or violating its canonical app target.
   0.3. **Package Standards Conformance (RULE 68 — BẮT BUỘC TUÂN THỦ .STANDARDS CỦA PACKAGE)**: All packages in `Packages/` must have a `.agents/standards/` (or `.agents/standards/`) directory containing `structure.md` and `designs.md`. When creating, refactoring, or updating any file in `Packages/{name}/`, developers and AI agents MUST strictly comply with `Packages/{name}/.agents/standards/structure.md` (directory organization, aliases, architectural rules) and `Packages/{name}/.agents/standards/designs.md` (theme tokens, 8-pt grid, bases, zero inline styles, zero raw emojis).
   0.4. **Follow & Keep Target of Project via .agents/PRODUCT_TARGET.md (RULE 69 — BẮT BUỘC TUÂN THỦ VÀ DUY TRÌ MỤC TIÊU DỰ ÁN TRONG .AGENTS/PRODUCT_TARGET.MD / PROJECT_TARGET.MD)**: Before creating, modifying, refactoring, or adding features to any package or root workspace, developers and AI agents MUST read `.agents/PRODUCT_TARGET.md` (at root: `.agents/PRODUCT_TARGET.md` / `PROJECT_TARGET.md` and `Packages/{packageName}/PROJECT_TARGET.md`) to guarantee 100% alignment with the package's canonical target and strict anti-goals. Strictly prohibited to introduce target drift, cross-contaminate package boundaries, or implement features outside of a package's designated scope. <!-- Legacy Alias: PROJECT_TARGET.md -->
   0.5. **Path & Directory Configuration via .env (RULE 72 — BẮT BUỘC ĐỊNH NGHĨA TOÀN BỘ PATH VÀ DIR TRONG .ENV — ZERO HARDCODED PATHS)**: All filesystem paths, storage roots (`FILES_STORAGE_PATH`, `APPS_STORAGE_PATH`, `CLOUD_STORAGE_PATH`, `COMPUTER_STORAGE_PATH`), build outputs (`APP_BUILD_PATHS`, `APP_ZIP_PATHS`, `APP_HUB_PATH`, `BUILD_DIR`, `BUILDS_HUB_DIR`), examples (`EXAMPLE_PROJECTS_PATH`), seed data (`USERS_DATA_PATH`), and remote deployment targets (`REMOTE_WORKSPACE_PATH`, `REMOTE_REBUILD_SCRIPT_PATH`) MUST be defined in `.env` and `.env.example`. Sourced dynamically via `process.env` with generic fallback (`os.homedir()`, `process.cwd()`). Strictly prohibited to hardcode user/machine paths (`/home/viettd`, `/home/tranduyviet`, `/home/user`, `/Users/...`) in source code, scripts, or engines.
1. **Queue & Feature Logging (RULE 67 — BẮT BUỘC LIST TOÀN BỘ QUEUE VÀO .AGENTS/FEATURES TRƯỚC KHI THỰC HIỆN VÀ FINISH TASK Ở QUEUE CUỐI CÙNG)**: For every batch, multi-task prompt, or incoming task queue, determine feature/bug type and list ALL items from the queue into `.agents/features/{dd-mm-yyyy}.md` (prioritizing current date) as uncompleted checkbox tasks `[ ]` (or `[ ] [BUG]`). Only at the last queue item (once the entire queue is completely logged into `.agents/features`), start executing and finishing the tasks. Strictly prohibited to start executing or marking tasks done before all queued items are recorded in `.agents/features` (or legacy `.agents/features`).
   1.1. **Git Task Branch Checkout (BẮT BUỘC KHI TẠO TASK)**: When creating a task, it is mandatory to checkout an isolated task branch branched from `tasks` (or specified parent branch) using the format `task/<task-id>-<slug>`:

```bash
git checkout tasks && git pull origin tasks && git checkout -b task/<task-id>-<slug>
```

All code edits, MCP definitions, engine logic, and UI components must be implemented on this task branch.
1.2. **Example File & Standards/Schema First (RULE 49 — BẮT BUỘC TRƯỚC KHI GENERATE FEATURE)**: When creating a new feature, new generator, or new data format, it is **MANDATORY** to have at least one example file first (in `.example/`, template fixture, or mock data), update core file handling logic (reader, writer, parser, validator), and update file standards and schemas in `.agents/standards/` (and `Lowcode/.agents/standards/MCP/schemas/`). Only then allow handling and feature generation strictly following the established standards of the file and schema.
1.3. **Bots & SuperChats API, Usecases, Models & Integration First (RULE 52 — BẮT BUỘC KHI TẠO FEATURE CHO BOTS & SUPERCHATS)**: When creating new features for `Bots` (`Packages/Bots`, `bot`) or `SuperChats` (`Packages/SuperChat`, `super-chat`), it is **100% MANDATORY** to only generate API with usecases, models (entities/DTOs/schemas), and integration first. Strictly prohibited to generate UI components before the backend API, usecases, models, and integration tests are passing and verified. 2. **MCP Standard First (BẮT BUỘC TRƯỚC TIÊN VỚI NEW FEATURE — MIỄN TRỪ VỚI FIX, MOVE FILE/FOLDER & COMPONENTS CHANGE)**:

- **MCP EXEMPTION RULE (MIỄN TRỪ VỚI FIX & MOVE FILE/FOLDER — ZERO TOLERANCE)**:
  If the task is a bug fix (bug / fix / hotfix), file/folder move or rename (move / rename file, folder), or component change (components change / UI modifications / styling / props / subcomponents), **TUYỆT ĐỐI KHÔNG triển khai vào MCP** (do not implement to MCP — do not add action to `actions.json`, do not create schema, do not create template, do not write MCP section in `standard-specification.md`). Skip MCP step and proceed directly to engine/UI implementation.
- **ELSE DO IT**: When creating a new feature, new tool, new action, new service, or new capability, it is **MANDATORY to register MCP definition in `Lowcode/.agents/standards/MCP/` (`actions.json`, `schemas/`, `templates/`, `standard-specification.md`) FIRST**.

3. **Core Engine First**: Finalize 100% core logic, state management, schema validation, data structures, and core services in `Lowcode/` (or `lc_engines/`) FIRST, strictly conforming to the validated file standards and schemas. UI layer serves purely as presentation and action dispatch.
4. **UI Implementation with .agents/standards & Theme**: After completing MCP (if new feature) and Core Engine, build UI with full reference to `Lowcode/.agents/standards/*` (IDE, UI, Projects, DB Engines), Theme tokens (`default-settings.json`, primary `#6938ef`, font `Inter`, 8-pt grid), and Base components defined in `Packages/Shared/components/{projectNames}/bases` (mapped via `@/components/bases` or `@Shared/components/{projectName}/bases`). When generating code, all base components MUST be defined in `Packages/Shared/components/{projectNames}`.
   4.1. **Check EOF on File Update (RULE 29 & SECTION 581)**: When updating any file, MUST verify EOF on that file first (exact 1 newline `\n` at end, properly closed brackets `{}` and JSX tags). Only proceed to next file once EOF check passes.
   4.2. **No Ad-Hoc Manual Testing During Edits — Testing is Automated on Git Commit (RULE 36 & SECTION 779 / RULE 36.1)**: Strictly avoid repetitive manual test runs (`npm test`, `vitest run`, `node scripts/fast-test.mjs`) during file updates. Automated tests execute on `git commit` via `.husky/pre-commit` hook (`fast-test --staged --fast --timeout=25 --commit-on-timeout`). If a test hangs (>25s) due to infinite loops, the runner cancels the test and allows commit to complete (exit 0) under Section 779 (Rule 36.1).
   4.3. **Always Add With Translation When Using New Components (RULE 54 & RULE 21)**: When consuming, instantiating, or using any new component in pages, layouts, or parent views, ALWAYS integrate it with translation (`useSafeTranslations`, wrap props and textual children with `t("key", "Default Text")`, zero hardcoded text strings).
   4.4. **Create New Components: Use Bases with Translation First (RULE 55 & RULE 50 & RULE 21)**: When creating any new component across all projects, MUST compose from Base components defined in `Packages/Shared/components/{projectNames}/bases` and MUST use translation first (`const t = useSafeTranslations("components.[componentName]")`, wrap all props/children with `t("key", "Default Text")`, zero raw unlocalized strings).
   4.5. **Next.js Projects Component Resolution Hierarchy: Use @Shared First, Then {projectName}/components (RULE 56)**: Across all Next.js projects, when creating projects or components, ALWAYS search and use components from `@Shared` first (`@Shared/components/{projectName}/bases`, `@Shared/components/bases`, `@Shared/components/theme`), and only use/define in `{projectName}/components` for project-specific unshared needs. Reusable components created locally must be promoted to `@Shared`.
   4.6. **Login First on Protected Next.js Applications & Routes (RULE 57)**: Across all Next.js projects, applications and routes categorized as private portals or workspaces (such as OfficePack, SuperChat, Clouds, Office, DeviceFarms, Bots, LowcodeStudio) MUST enforce authentication guards first via Edge middleware (`middleware.ts`), redirecting unauthenticated requests to login (`/${locale}/login` or `/login`) with `?redirect=<path>`. Zero tolerance for skipping auth guards in development mode.
   4.7. **All Packages Use Import with Alias @ on New Components (RULE 58)**: Across all packages in `Packages/`, when creating or scaffolding any new component, ALL module and component imports MUST use path aliases starting with `@` (`@/...`, `@Shared/...`, `@<PackageName>/...`). Strictly prohibited to use deep relative climbing imports (`../../...`) to access components, bases, lib, or utilities.
   4.8. **Always Move /{projectName}/components/bases to @Shared/components/{projectName}/bases (RULE 59)**: Across all packages in `Packages/`, whenever base components exist, are discovered, or are created in `/{projectName}/components/bases`, ALWAYS move them to `@Shared/components/{projectName}/bases` (`Packages/Shared/components/{projectName}/bases`), completely remove project-local `components/bases`, and update all imports to use canonical `@Shared/components/{projectName}/bases` (or mapped `@/components/bases`).
   4.9. **Office Toolbars Multi-Group Tabbed Ribbon Architecture (RULE 60 & RULE 55 ALIAS)**: Toolbars in Office editors must be structured into multiple functional groups displayed as tabs (Ribbon Tabbed Toolbar Architecture) allowing users to switch between cohesive tool groups seamlessly, avoiding flat monolithic sprawl.
   4.10. **Block Cross-Project Updates to Shared/components Bases (RULE 61 & RULE 51 ALIAS)**: All base components in `Shared/components/{projectName}/bases` belong exclusively to `{projectName}`; developers and AI agents are strictly prohibited from modifying or importing base components across project boundaries.
   4.11. **Fetching Data from Local Must Use Real Directory (RULE 62 & SECTION 825 & RULE 50 ALIAS)**: All file and directory operations for local storage must resolve real authentic filesystem paths and real file content; zero fabricated dummy mock data.
   4.12. **All Apps in Packages Use Lowcode via gRPC with Zero Direct Logic Imports — Lowcode Runs as Hub Service in Sleep Mode by Default (RULE 66)**: All applications across `Packages/` must interact with Lowcode exclusively via package-local gRPC clients (`@/lib/grpc` or `@/app/api/_grpc`). `@Shared` must no longer be used for gRPC calls. Strictly prohibited to import runtime logic/engines directly from `@Lowcode/sources/...`. Lowcode runs as an instance service managed under Hub, auto-started on Hub launch in default sleep mode (standby) and waking on call.
5. **Auto Commit on Task Branch**: Commit code on task branch with Conventional Commits (`feat:`, `fix:`, `refactor:`...). Pre-commit hook executes tests on staged changes. **DO NOT RUN TYPE-CHECK ON COMMIT** (type-check runs exclusively on push code under Rule 28).
6. **Task Done — Quay Về Parent Branch, Resolve Conflict & Merge (BẮT BUỘC KHI DONE)**: When task is complete, return to parent branch (`git checkout <parent-branch>`), pull latest updates (`git pull origin <parent-branch>`), resolve conflicts, and merge task branch (`last_branch`):

```bash
git checkout tasks
git merge <task-branch>
```

Run fast unit tests to verify zero regressions. **DO NOT RUN TYPE-CHECK IN THIS MERGE STEP**.
6.1. **Kill Port 3020 on Close Task (RULE 63 — BẮT BUỘC KHI CLOSE TASK / DONE TASK)**: When closing or completing any task, it is 100% mandatory to kill port 3020 (PORT of app: 2-TEK Hub) via `node scripts/kill-port-3020.mjs` or `fuser -k -9 3020/tcp 2>/dev/null || true` to prevent lingering Next.js dev server processes from remaining bound to port 3020. 7. **Push Code (EXCLUSIVE STAGE FOR TYPE-CHECK — RULE 27 & RULE 28)**: After merge and passing tests, push merged code to remote (`git push origin <parent-branch>`). Hook `.husky/pre-push` is the single gate executing TypeScript typecheck (`npm run type-check:fast`). If working tree has uncommitted changes, push and typecheck are canceled (staged as commit only). Supports `SKIP_TYPECHECK=1` or `CANCEL_TYPECHECK=1`. 8. **Post-Push Builder Workflow (RULE 38 & RULE 41 & SECTION 709)**: Pushing code does not trigger ad-hoc local Docker builds. All packaging and release builds are delegated exclusively to Builder Bot.
